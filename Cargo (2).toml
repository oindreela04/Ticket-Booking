[package]
name = "ticket-booking"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <you@example.com>"]
description = "Soroban smart contract for decentralized ticket booking on Stellar"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.0.0" }

[dev-dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
